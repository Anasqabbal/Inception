server {
        listen 8080 default_server;
		root /var/www/html/;
		index index.html;
        # Everything is a 404

        location / {
			index index.html;
        }

        # You may need this to prevent return 404 recursion.
        location = /404.html {
                internal;
        }
}